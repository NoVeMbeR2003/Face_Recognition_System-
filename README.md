# Face_Recognition_System-
This project is a simple and efficient Face Recognition System built using Python and OpenCV. It can detect faces from a webcam, collect datasets, train a model, and recognize individuals in real time.  The system uses the LBPH (Local Binary Pattern Histogram) algorithm for face recognition, which is lightweight, fast, and suitable for beginners.
